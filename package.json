{
  "name": "todo-microservices",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "services/*",
    "frontend",
    "lambdas/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "migrate:local": "npm -w @todo/auth-service run prisma:migrate && npm -w @todo/todo-service run prisma:migrate && npm -w @todo/file-service run prisma:migrate",
    "seed:local": "npm -w @todo/auth-service run seed && npm -w @todo/todo-service run seed && npm -w @todo/file-service run seed",
    "migrate:docker": "docker compose run --rm --no-deps auth-service npm -w @todo/auth-service run prisma:migrate && docker compose run --rm --no-deps todo-service npm -w @todo/todo-service run prisma:migrate && docker compose run --rm --no-deps file-service npm -w @todo/file-service run prisma:migrate",
    "seed:docker": "docker compose run --rm --no-deps auth-service npm -w @todo/auth-service run seed && docker compose run --rm --no-deps todo-service npm -w @todo/todo-service run seed && docker compose run --rm --no-deps file-service npm -w @todo/file-service run seed",
    "migrate:exec": "docker compose exec -T auth-service npm -w @todo/auth-service run prisma:migrate && docker compose exec -T todo-service npm -w @todo/todo-service run prisma:migrate && docker compose exec -T file-service npm -w @todo/file-service run prisma:migrate",
    "seed:exec": "docker compose exec -T auth-service npm -w @todo/auth-service run seed && docker compose exec -T todo-service npm -w @todo/todo-service run seed && docker compose exec -T file-service npm -w @todo/file-service run seed",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/bcryptjs": "^2.4.6",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/morgan": "^1.9.10",
    "@types/multer": "^2.1.0",
    "@types/node-cron": "^3.0.11",
    "@types/pg": "^8.20.0",
    "eslint": "^9.17.0",
    "jsdom": "^29.1.1",
    "prettier": "^3.4.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.2"
  }
}
